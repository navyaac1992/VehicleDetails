This is a spring boot application.It is using h2 as its in-memory database

To run this you need Java and Maven installed. 

Steps : 

1.Download project and navigate to directory.
2.Build the project using "mvn clean install"
3.Run the application using "mvn spring-boot:run"

The server starts on port 8080 

The client can accesed using localhost:8080 

